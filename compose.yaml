services:
  awsl-remotex:
    image: ${AWSL_REMOTEX_IMAGE:-awsl-remotex:local}
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      AUTH_USERNAME: ${AUTH_USERNAME:-admin}
      AUTH_PASSWORD: ${AUTH_PASSWORD:-}
      CREDENTIAL_KEY: ${CREDENTIAL_KEY:?set CREDENTIAL_KEY to 64 hexadecimal characters}
      GUACAMOLE_JSON_SECRET: ${GUACAMOLE_JSON_SECRET:?set GUACAMOLE_JSON_SECRET to 32 hexadecimal characters}
      GUACAMOLE_UPSTREAM: http://guacamole:8080
      GUACD_ADDRESS: guacd:4822
      SESSION_IDLE_TIMEOUT: ${SESSION_IDLE_TIMEOUT:-24h}
    depends_on:
      - guacamole
    restart: unless-stopped

  guacd:
    image: ghcr.io/dreamhunter2333/awsl-remotex-guacd:1.6.1
    restart: unless-stopped

  guacamole:
    image: guacamole/guacamole:1.6.0
    depends_on:
      - guacd
    environment:
      GUACD_HOSTNAME: guacd
      JSON_ENABLED: "true"
      JSON_SECRET_KEY: ${GUACAMOLE_JSON_SECRET:?set GUACAMOLE_JSON_SECRET to 32 hexadecimal characters}
      API_SESSION_TIMEOUT: ${GUACAMOLE_SESSION_TIMEOUT_MINUTES:-1440}
    restart: unless-stopped

  pve-vnc-proxy:
    profiles: ["pve"]
    image: ghcr.io/dreamhunter2333/pve-vnc-proxy:v0.1.0
    environment:
      PVE_HOST: ${PVE_HOST:-}
      PVE_LISTEN: 0.0.0.0:5900
      PVE_INSECURE: ${PVE_INSECURE:-false}
      PVE_MAX_CONNS: ${PVE_MAX_CONNS:-256}
    restart: unless-stopped
